# GENIVI Packages
IMAGE_INSTALL += "dlt-daemon dlt-daemon-systemd"
IMAGE_INSTALL += "node-state-manager"
IMAGE_INSTALL += "persistence-client-library"

# Comment out audiomanager since it is broken with CommonAPI 2.1.6
#IMAGE_INSTALL += "audiomanager"
